<% if flash[:error] %>
  <div class="alert alert-error reservation-errors">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:error] %>
  </div>
<% end %>

<div id="error-message">
</div>

<%= form_for customer, :url => customers_path(customer), :method => 'post', :html => {:id => "new-customer"} do |f| %>
  <%= render :partial => 'account', :locals => {:f => f, :customer => customer} %>
<% end %>
