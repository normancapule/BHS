<% if flash[:error] %>
  <div class="alert alert-error reservation-errors">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:error] %>
  </div>
<% end %>

<div id="error-message">
</div>

<%= form_for package, :url => package_path(package), :method => 'put', :html => {:id => "edit-package-form"} do |f| %>
  <%= render :partial => 'package', :locals => {:f => f, :package => package} %>
<% end %>
