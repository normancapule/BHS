<% if flash[:error] %>
  <div class="alert alert-error reservation-errors">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:error] %>
  </div>
<% end %>

<div id="error-message">
</div>

<%= form_for service, :url => service_path(service), :method => 'put', :html => {:id => "edit-service-form"} do |f| %>
  <%= render :partial => 'service', :locals => {:f => f, :service => service} %>
<% end %>
