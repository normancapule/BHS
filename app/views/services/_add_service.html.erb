<% if flash[:error] %>
  <div class="alert alert-error reservation-errors">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:error] %>
  </div>
<% end %>

<div id="error-message">
</div>

<%= form_for service, :url => services_path(service), :method => 'post', :html => {:id => "new-service"} do |f| %>
  <%= render :partial => 'service', :locals => {:f => f, :service => service} %>
<% end %>
