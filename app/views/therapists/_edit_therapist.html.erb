<% if flash[:error] %>
  <div class="alert alert-error reservation-errors">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:error] %>
  </div>
<% end %>

<div id="error-message">
</div>

<%= form_for therapist, :url => therapist_path(therapist.id), :method => 'put', :html => {:id => "edit-therapist-form"} do |f| %>
  <%= render :partial => 'account', :locals => {:f => f, :therapist => therapist} %>
<% end %>

