<% if flash[:notification] %>
  <div class="alert alert-success">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <%= flash[:notification] %>
  </div>
<% end %>

<div class="table-headers">
  <span class="label">Reservations for <%= date %> </span>
  <%= text_field_tag :date, "", :id => "reservation-date-selector", :class => "hidden" %>
  <a class="btn btn-small add-dialog"><i class="icon-plus"></i></a>
</div>
<table id="reservation-table" class="table table-striped table-bordered table-hover" data-source="<%= reservations_url(format: 'json', date: params[:date]) %>">
  <thead>
    <th>Name</th>
    <th>Number of People</th>
    <th>Time</th>
    <th></th>
  </thead>
  <tbody>
  </tbody>
</table>
